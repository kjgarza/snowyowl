# Configuration for SnowyOwl Automation

# Root directory containing repositories to process
SNOWYOWL_ROOT="${HOME}/aves"

# Base branch for pull requests
SNOWYOWL_BASE_BRANCH="main"

# Log directory
SNOWYOWL_LOG_DIR="${HOME}/aves/labs/snowyowl/logs"

# GitHub CLI settings
SNOWYOWL_GH_TIMEOUT=300  # Timeout for gh commands in seconds

# Git settings
SNOWYOWL_GIT_USER_NAME=""  # Leave empty to use git global config
SNOWYOWL_GIT_USER_EMAIL="" # Leave empty to use git global config

# Copilot CLI settings (for future integration)
SNOWYOWL_COPILOT_TIMEOUT=600  # Timeout for copilot commands in seconds

# PR settings
SNOWYOWL_PR_LABEL="snowyowl-automation"  # Label to add to PRs
SNOWYOWL_PR_DRAFT=false  # Create PRs as drafts
SNOWYOWL_PR_AUTO_MERGE=false  # Enable auto-merge for PRs

# Task processing
SNOWYOWL_MAX_TASKS_PER_RUN=10  # Maximum tasks to process per repository
SNOWYOWL_SKIP_REPOS=""  # Comma-separated list of repos to skip

# Notification settings (future feature)
SNOWYOWL_NOTIFY_EMAIL=""  # Email for notifications
SNOWYOWL_NOTIFY_SLACK_WEBHOOK=""  # Slack webhook URL
