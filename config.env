# Configuration for SnowyOwl Automation

# Root directory containing repositories to process
SNOWYOWL_ROOT="${HOME}/aves"

# Base branch for pull requests
SNOWYOWL_BASE_BRANCH="main"

# Log directory
SNOWYOWL_LOG_DIR="${HOME}/aves/snowyowl/logs"

# Worktrees directory (for isolated git worktrees)
SNOWYOWL_WORKTREES_DIR="${HOME}/aves/worktrees"

# =============================================================================
# AI Backend Configuration
# =============================================================================

# AI backend to use: "copilot" or "claude"
# Can also be set via --backend/-B command line flag
SNOWYOWL_BACKEND="copilot"

# AI model to use for code generation
# Can also be set via --model/-m command line flag
# Default for copilot: gpt-4o
# Default for claude: claude-sonnet-4-5-20250514
# Other options:
#   For copilot: gpt-4, gpt-4-turbo, gpt-3.5-turbo, o1-preview, o1-mini
#   For claude: claude-sonnet-4-5-20250514, claude-3-5-sonnet-20241022, claude-3-opus-20240229
SNOWYOWL_MODEL="claude-sonnet-4-5"

# Claude Code CLI settings (used when SNOWYOWL_BACKEND=claude)
# Tools that Claude is allowed to use
CLAUDE_ALLOWED_TOOLS="Read,Write,Edit,Bash,Grep,Glob"
# Permission mode for file edits: acceptEdits, ask, plan
CLAUDE_PERMISSION_MODE="acceptEdits"

# Copilot CLI settings (used when SNOWYOWL_BACKEND=copilot)
# Tools/commands to deny (safety restriction)
COPILOT_DENY_TOOLS="shell(rm)"

# =============================================================================
# GitHub CLI settings
# =============================================================================

SNOWYOWL_GH_TIMEOUT=300  # Timeout for gh commands in seconds

# Git settings
SNOWYOWL_GIT_USER_NAME=""  # Leave empty to use git global config
SNOWYOWL_GIT_USER_EMAIL="" # Leave empty to use git global config

# Copilot CLI settings (legacy - use SNOWYOWL_BACKEND instead)
SNOWYOWL_COPILOT_TIMEOUT=600  # Timeout for copilot commands in seconds

# =============================================================================
# PR settings
# =============================================================================

SNOWYOWL_PR_LABEL="snowyowl-automation"  # Label to add to PRs
SNOWYOWL_PR_DRAFT=false  # Create PRs as drafts
SNOWYOWL_PR_AUTO_MERGE=false  # Enable auto-merge for PRs
SNOWYOWL_CREATE_PR=false  # Enable PR creation (default: false - only commit locally)

# =============================================================================
# Task processing
# =============================================================================

SNOWYOWL_MAX_TASKS_PER_RUN=10  # Maximum tasks to process per repository
SNOWYOWL_SKIP_REPOS=""  # Comma-separated list of repos to skip

# =============================================================================
# Notification settings (future feature)
# =============================================================================

SNOWYOWL_NOTIFY_EMAIL=""  # Email for notifications
SNOWYOWL_NOTIFY_SLACK_WEBHOOK=""  # Slack webhook URL
